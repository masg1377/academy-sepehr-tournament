import React, { FC } from "react";
import { ClientsDetails } from "@src/components/ClientsRequests/ClientsDetails";

const Detail: FC = (): JSX.Element => {
  return <ClientsDetails />;
};

export { Detail };
