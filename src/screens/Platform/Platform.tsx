import React, { FC } from "react";
import { Platform as PlatformComponent } from "@src/components/Platform/Platform";

const Platform: FC = (): JSX.Element => {
  return <PlatformComponent />;
};

export { Platform };
