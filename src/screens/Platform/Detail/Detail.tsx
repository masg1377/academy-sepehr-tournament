import React, { FC } from "react";
import { PlatformDetail } from "@src/components/Platform/PlatformDetail";

const Detail: FC = (): JSX.Element => {
  return <PlatformDetail />;
};

export { Detail };
