import React, { FC } from "react";
import { PermissionContainer } from "@src/components/PermissionContainer";

const Permissions: FC = (): JSX.Element => {
  return <PermissionContainer />;
};

export { Permissions };
