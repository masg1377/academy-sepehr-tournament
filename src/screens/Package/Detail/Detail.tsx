import React, { FC } from "react";
import { PackageDetail } from "@src/components/PackageContainer/PackageDetail";

const Detail: FC = (): JSX.Element => {
  return <PackageDetail />;
};

export { Detail };
