import React, { FC } from "react";
import { PaymentHistory as CustomerPaymenyHistory } from "@src/components/CustomersComponent/PaymentHistory/PaymentHistory";

const PaymentHistory: FC = (): JSX.Element => {
  return <CustomerPaymenyHistory />;
};

export { PaymentHistory };
