import { PublisherComponent } from "@src/components/PublisherComponent/PublisherComponent";
import React, { FC } from "react";

const Publisher: FC = (): JSX.Element => {
  return <PublisherComponent />;
};

export { Publisher };
