import { ListContainer as PromotionsListContainer } from "@src/components/PromotionsContainer/ListContainer/ListContainer";
import React, { FC } from "react";

const PromotionsList: FC = (): JSX.Element => {
  return <PromotionsListContainer />;
};

export { PromotionsList };
